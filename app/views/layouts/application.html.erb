<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <script src="https://kit.fontawesome.com/e3debef22c.js" crossorigin="anonymous"></script>
      <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
            <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <% if user_signed_in? %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark py-1">
          <ul class="navbar-brand">
             <%= link_to "Bookers", root_path, class: "nav-link text-light" %>
          </ul>
          <div class="container-fluid">
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                  <i class="fa-solid fa-house" style="color: #ffffff;"></i><%= link_to "Home", user_path(current_user), class: "nav-link text-light" %>
            </div>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                <i class="fa-solid fa-users", style="color: #ffffff;"></i><%= link_to "Users", users_path, class: "nav-link text-light" %>
              </div>
            </ul>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                <i class="fa-solid fa-book-open", style="color: #ffffff;"></i><%= link_to "Books", books_path, class: "nav-link text-light" %>
              </div>
            </ul>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                <i class="fa-solid fa-right-from-bracket", style="color: #ffffff;"></i><%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link text-light" %>
              </div>
            </ul>
          </div>
        </nav>
      <% else %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark py-1">
          <ul class="navbar-brand">
             <%= link_to "Bookers", root_path, class: "nav-link text-light" %>
          </ul>
          <div class="container-fluid">
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                  <i class="fa-solid fa-house" style="color: #ffffff;"></i><%= link_to "Home", root_path, class: "nav-link text-light" %>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                  <i class="fa-solid fa-link" style="color: #ffffff;"></i><%= link_to "About", about_path, class: "nav-link text-light" %>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                  <i class="fa-solid fa-user-plus" style="color: #ffffff;" ></i><%= link_to "Sign up", new_user_registration_path, class: "nav-link text-light" %>
            <ul class="navbar-nav ml-auto">
              <div style="display: flex; align-items: center;">
                  <i class="fa-solid fa-right-to-bracket" style="color: #ffffff;" ></i><%= link_to "Log in", new_user_session_path, class: "nav-link text-light" %>
          </div>
        </nav>
      <% end %>
    </header>
    
    <% unless (controller_name == "sessions" && action_name == "new") %> 
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key %>">
          <%= message.html_safe %>
        </div>
      <% end %>
    <% end %>

    <%= yield %>
  </body>

  
    <div class="d-flex justify-content-center">
    <footer>
      CopyRight Infratop.inc
    </footer>
    </div>
  
</html>

